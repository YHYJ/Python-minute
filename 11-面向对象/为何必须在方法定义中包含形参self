为何必须在方法定义中包含形参self呢？

因为Python调用__init__()方法来创建Dog实例时，将自动传入实参self。
每个与类相关联的方法调用都自动传递实参self，它是一个指向实例本身的引用，让实例能够访问类中的属性和方法.
创建Dog实例时， Python将调用Dog类的方法__init__(),self会自动传递，因此我们不需要传递它.
每当我们根据Dog类创建实例时，都只需给最后两个形参（name和age）提供值.
